'use strict'

const mathematic = require ('.')


const a=8, b=4
console.log(a +"  + "+ b+  " = " + mathematic.add(a,b))
console.log(a +"  - "+ b+  " = " + mathematic.sub(a,b))
console.log(a +"  / "+ b+  " = " + mathematic.div(a,b))
console.log(a +"  * "+ b+  " = " + mathematic.mul(a,b))